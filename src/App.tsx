import React from 'react';
import Router from './components/Router';

const App: React.FC = () => {
  return (
    <div className="Contenedor">
      <Router />
    </div>
  );
};

export default App;
